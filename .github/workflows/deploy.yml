# ClimaAI CI/CD Pipeline
# Deploy to Google Cloud Run on push to main

name: Deploy to Production

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
  workflow_dispatch:

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  REGION: us-central1
  SERVICE_NAME: climaai-api
  REGISTRY: gcr.io

jobs:
  # ==========================================================================
  # Build and Test
  # ==========================================================================
  test:
    runs-on: ubuntu-latest

    # The suite runs against real Postgres, not SQLite: several handlers use
    # Postgres-only SQL (jsonb casts, `= ANY(:array)`) that SQLite cannot run,
    # so a SQLite run would pass while saying nothing about production.
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: climaai
          POSTGRES_PASSWORD: climaai123
          POSTGRES_DB: climaai
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          cd backend/api
          pip install -r requirements.txt
          pip install pytest pytest-asyncio httpx

      - name: Run tests
        run: |
          cd backend/api
          python -m pytest tests/ -v
        env:
          # conftest.py creates the test database and applies init.sql and
          # 002_add_features.sql to it.
          TEST_DATABASE_URL: postgresql://climaai:climaai123@localhost:5432/climaai_test
          DATABASE_URL: postgresql+asyncpg://climaai:climaai123@localhost:5432/climaai
          REDIS_URL: redis://localhost:6379/0
          JWT_SECRET: test-secret-key
          OPENAI_API_KEY: test-key
          # Makes an unreachable database a hard failure rather than a skip.
          CI: "true"

  # ==========================================================================
  # Build Docker Image
  # ==========================================================================
  build:
    needs: test
    runs-on: ubuntu-latest
    outputs:
      image: ${{ steps.build.outputs.image }}
      
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
          
      - name: Configure Docker for GCR
        run: gcloud auth configure-docker
        
      - name: Build and Push
        id: build
        run: |
          IMAGE="${REGISTRY}/${PROJECT_ID}/${SERVICE_NAME}:${{ github.sha }}"
          docker build -t ${IMAGE} ./backend/api
          docker push ${IMAGE}
          echo "image=${IMAGE}" >> $GITHUB_OUTPUT

  # ==========================================================================
  # Deploy to Cloud Run
  # ==========================================================================
  deploy:
    needs: build
    runs-on: ubuntu-latest
    
    steps:
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
          
      - name: Deploy to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: ${{ env.SERVICE_NAME }}
          image: ${{ needs.build.outputs.image }}
          region: ${{ env.REGION }}
          env_vars: |
            ENVIRONMENT=production
            DEBUG=false
          secrets: |
            DATABASE_URL=DATABASE_URL:latest
            REDIS_URL=REDIS_URL:latest
            OPENAI_API_KEY=OPENAI_API_KEY:latest
            JWT_SECRET=JWT_SECRET:latest
          flags: |
            --allow-unauthenticated
            --min-instances=1
            --max-instances=10
            --memory=1Gi
            --cpu=1
            --concurrency=80
            --timeout=60s

      - name: Show URL
        run: |
          echo "Deployed to: https://${SERVICE_NAME}-${PROJECT_ID}.run.app"

  # ==========================================================================
  # Post-Deploy Health Check
  # ==========================================================================
  health-check:
    needs: deploy
    runs-on: ubuntu-latest
    
    steps:
      - name: Wait for deployment
        run: sleep 30
        
      - name: Health Check
        run: |
          response=$(curl -s -o /dev/null -w "%{http_code}" \
            https://${SERVICE_NAME}-${PROJECT_ID}.run.app/health)
          if [ "$response" != "200" ]; then
            echo "Health check failed with status: $response"
            exit 1
          fi
          echo "Health check passed!"
