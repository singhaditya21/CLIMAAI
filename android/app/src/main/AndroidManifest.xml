<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- There is no widget foreground service. This is WorkManager's: on API < 31
         it runs expedited work (WorkScheduler.triggerImmediateRefresh) inside its
         own SystemForegroundService. WorkManager merges the permission in itself;
         it is repeated here so the requirement is visible. No typed
         FOREGROUND_SERVICE_* permission is needed — that requirement only applies
         to services started on API 34+ devices, and on those the same work runs as
         an expedited job with no service involved. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- android:enableOnBackInvokedCallback below: at targetSdk 36 Android stops
         dispatching onBackPressed() and KEYCODE_BACK and drives back through
         OnBackInvokedCallback whether or not this is declared. Declaring it makes
         API 33-35 devices behave the same way instead of diverging from Android 16.
         Safe here because nothing overrides onBackPressed() — back is handled by
         Navigation Compose through OnBackPressedDispatcher, which registers the
         platform callback for us. -->
    <application
        android:name=".ClimaAIApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClimaAI"
        android:enableOnBackInvokedCallback="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ClimaAI"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Widget Configuration Activity -->
        <activity
            android:name=".widget.WidgetConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.ClimaAI">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
        
        <!-- ============================================================ -->
        <!-- Weather Widgets -->
        <!-- ============================================================ -->
        
        <!-- Small Weather Widget (2x1) -->
        <receiver
            android:name=".widget.SmallWeatherWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_small_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/small_weather_widget_info" />
        </receiver>
        
        <!-- Medium Weather Widget (3x2) -->
        <receiver
            android:name=".widget.MediumWeatherWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_medium_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/medium_weather_widget_info" />
        </receiver>
        
        <!-- Large Weather Widget (4x3) -->
        <receiver
            android:name=".widget.LargeWeatherWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_large_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/large_weather_widget_info" />
        </receiver>
        
        <!-- Extra Large Weather Widget (4x4) with 7-day forecast -->
        <receiver
            android:name=".widget.ExtraLargeWeatherWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_xlarge_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/xlarge_weather_widget_info" />
        </receiver>
        
        <!-- ============================================================ -->
        <!-- Boot Receiver for re-scheduling work after device restart -->
        <!-- ============================================================ -->
        
        <receiver
            android:name=".work.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        
        <!-- Google Play Billing. Play reads this to decide which billing flows the
             app supports; it must match the billing-ktx version in app/build.gradle
             or the upload is rejected for the version it claims here. -->
        <meta-data
            android:name="com.google.android.play.billingclient.version"
            android:value="8.0.0" />
    </application>

</manifest>
