## 2024-05-09 - [Focus States using Existing Styles]
**Learning:** For enforcing the "no custom CSS" rule while ensuring keyboard accessibility, grouping the `:focus-visible` pseudo-class alongside existing `:hover` style blocks is an effective and safe approach.
**Action:** Next time, remember to combine `:focus-visible` with `:hover` selectors in legacy CSS to provide necessary keyboard feedback without adding new UI classes or deviating from existing design patterns.
